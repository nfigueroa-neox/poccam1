# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── Datos generados por el WORKER ────────────────────────
# (viven dentro de worker/, que es donde corre el proceso)
worker/capturas_cambio/
worker/analisis_ia/
worker/eventos.jsonl
worker/roi.json
worker/ia_prompts_hist.json

# ── Datos generados por el CONCENTRADOR ──────────────────
concentrador/pendientes.jsonl

# ── Backend nube (Node) ──────────────────────────────────
backend-nube/node_modules/
backend-nube/.vercel/
backend-nube/.env
backend-nube/.env.local

# ── Archivos de prueba / diagnóstico ─────────────────────
tests/_test_cola.jsonl
log_*.txt
diagnostico/
prueba_*.png

# ── IDEs y sistema ───────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Credenciales y secretos (nunca subir) ────────────────
.env
*.env
*.key
!backend-nube/.env.example
